<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataBackup"
        tools:targetApi="31">
        <activity
            android:name=".activity.processing.ProcessingBackupAppActivity"
            android:exported="false" />
        <activity
            android:name=".activity.processing.ProcessingBackupMediaActivity"
            android:exported="false" />
        <activity
            android:name=".activity.processing.ProcessingRestoreAppActivity"
            android:exported="false" />
        <activity
            android:name=".activity.processing.ProcessingRestoreMediaActivity"
            android:exported="false" />
        <activity
            android:name=".activity.list.AppListBackupActivity"
            android:exported="false" />
        <activity
            android:name=".activity.list.AppListRestoreActivity"
            android:exported="false" />
        <activity
            android:name=".activity.guide.GuideActivity"
            android:exported="true"
            android:theme="@style/Theme.DataBackup.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.main.MainActivity"
            android:exported="false" />
    </application>

</manifest>